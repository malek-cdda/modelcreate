"use client";
import React, { useState } from "react";

const Home = () => {
  const script = document.createElement("script");
  // use local file
  // script.src = 'script.js';

  const buttons = document.createElement("button");

  return (
    <div>
      this is name i create so over the rules nice to me
      <button>function off</button>
    </div>
  );
};

export default Home;
